---
/**
 * KaTeX stylesheet for math posts only.
 * Formulas are already HTML from remark-math + rehype-katex (astro.config).
 * Render this component only when `math: true` so CSS is not on every page.
 */
import "katex/dist/katex.min.css";
---

{/* side-effect CSS import; no markup */}
