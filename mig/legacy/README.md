# Legacy Jekyll artifacts

Archived during Astro cutover (M4). Not used by production build.

| File | Notes |
|------|--------|
| `_config.yml` | Pre-Astro Jekyll site config; mapped into `src/data/site.ts` |

Posts may still exist at repo-root `_posts/` until M5 cleanup; live content is `src/content/posts/`.
